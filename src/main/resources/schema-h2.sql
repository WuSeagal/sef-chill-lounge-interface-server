-- for SESSION(PRD不會自己建)
-- 子表先 DROP（FK 依賴關係：SPRING_SESSION_ATTRIBUTES -> SPRING_SESSION）
DROP TABLE IF EXISTS SPRING_SESSION_ATTRIBUTES;
DROP TABLE IF EXISTS SPRING_SESSION;
CREATE TABLE SPRING_SESSION (
    PRIMARY_ID CHAR(36) NOT NULL,
    SESSION_ID CHAR(36) NOT NULL,
    CREATION_TIME BIGINT NOT NULL,
    LAST_ACCESS_TIME BIGINT NOT NULL,
    MAX_INACTIVE_INTERVAL INT NOT NULL,
    EXPIRY_TIME BIGINT NOT NULL,
    PRINCIPAL_NAME VARCHAR(100),
    CONSTRAINT SPRING_SESSION_PK PRIMARY KEY (PRIMARY_ID)
);

CREATE UNIQUE INDEX SPRING_SESSION_IX1 ON SPRING_SESSION(SESSION_ID);
CREATE INDEX SPRING_SESSION_IX2 ON SPRING_SESSION(EXPIRY_TIME);
CREATE INDEX SPRING_SESSION_IX3 ON SPRING_SESSION(PRINCIPAL_NAME);

CREATE TABLE SPRING_SESSION_ATTRIBUTES (
    SESSION_PRIMARY_ID CHAR(36)     NOT NULL,
    ATTRIBUTE_NAME     VARCHAR(200) NOT NULL,
    ATTRIBUTE_BYTES    BYTEA        NOT NULL,
    CONSTRAINT SPRING_SESSION_ATTRIBUTES_PK PRIMARY KEY (SESSION_PRIMARY_ID, ATTRIBUTE_NAME),
    CONSTRAINT SPRING_SESSION_ATTRIBUTES_FK FOREIGN KEY (SESSION_PRIMARY_ID)
    REFERENCES SPRING_SESSION (PRIMARY_ID) ON DELETE CASCADE
);

CREATE INDEX SPRING_SESSION_ATTRIBUTES_IX1 ON SPRING_SESSION_ATTRIBUTES (SESSION_PRIMARY_ID);

-- ADMIN_USER
DROP TABLE IF EXISTS ADMIN_USER;
CREATE TABLE ADMIN_USER (
    id                  UUID PRIMARY KEY,
    provider_user_id    VARCHAR(64) NOT NULL,
    email               VARCHAR(256),
    google_name         VARCHAR(256),
    role_name           VARCHAR(64) NOT NULL,
    enabled             BOOLEAN NOT NULL DEFAULT TRUE,
    first_login         BOOLEAN NOT NULL DEFAULT TRUE,
    banned              BOOLEAN NOT NULL DEFAULT FALSE,
    created_date        TIMESTAMP,
    last_modified_date  TIMESTAMP,
    CONSTRAINT unique_provider_user_id UNIQUE (provider_user_id)
);

-- ATTENDEE_DATA
DROP TABLE IF EXISTS ATTENDEE_DATA;
CREATE TABLE ATTENDEE_DATA (
    id                  BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id             VARCHAR(64) NOT NULL,
    username            VARCHAR(256),
    fur_name            VARCHAR(256),
    avatar              VARCHAR(512),
    avatar_color        VARCHAR(16),
    topic_id            VARCHAR(64),
    created_date        TIMESTAMP,
    last_modified_date  TIMESTAMP,
    CONSTRAINT unique_attendee_user_id UNIQUE (user_id)
);

-- TAG
DROP TABLE IF EXISTS TAG;
CREATE TABLE TAG (
    id                  BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    tag_id              VARCHAR(64) NOT NULL,
    type                VARCHAR(64) NOT NULL,
    content             VARCHAR(256) NOT NULL,
    created_date        TIMESTAMP,
    last_modified_date  TIMESTAMP,
    CONSTRAINT unique_tag_id UNIQUE (tag_id)
);

-- ATTENDEE_TAG
DROP TABLE IF EXISTS ATTENDEE_TAG;
CREATE TABLE ATTENDEE_TAG (
    id                  BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id             VARCHAR(64) NOT NULL,
    tag_id              VARCHAR(64) NOT NULL,
    created_date        TIMESTAMP,
    CONSTRAINT unique_attendee_tag UNIQUE (user_id, tag_id)
);

-- ATTENDEE_SOCIAL
DROP TABLE IF EXISTS ATTENDEE_SOCIAL;
CREATE TABLE ATTENDEE_SOCIAL (
    id                  BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id             VARCHAR(64) NOT NULL,
    platform            VARCHAR(64) NOT NULL,
    links               VARCHAR(512) NOT NULL,
    created_date        TIMESTAMP,
    last_modified_date  TIMESTAMP
);

-- ATTENDEE_STICKER
DROP TABLE IF EXISTS ATTENDEE_STICKER;
CREATE TABLE ATTENDEE_STICKER (
    id                  BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id             VARCHAR(64) NOT NULL,
    sticker_no          INT NOT NULL,
    sticker             VARCHAR(512),
    created_date        TIMESTAMP,
    last_modified_date  TIMESTAMP,
    CONSTRAINT unique_sticker_slot UNIQUE (user_id, sticker_no)
);

-- TOPIC
DROP TABLE IF EXISTS TOPIC;
CREATE TABLE TOPIC (
    id                  BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    topic_id            VARCHAR(64) NOT NULL,
    content             VARCHAR(1024) NOT NULL,
    created_date        TIMESTAMP,
    last_modified_date  TIMESTAMP,
    CONSTRAINT unique_topic_id UNIQUE (topic_id)
);
